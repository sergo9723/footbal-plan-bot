# Аудит №39 (v333) — 2 прохода поверх v332, дочитал файлы до конца

По вашей просьбе прогнал код ещё раз, 2 прохода, и впервые дочитал до конца два файла, которые в прошлый раз просмотрел частично: `bot_knowledge.json` (все 71 сделка, а не первые ~38) и выгрузку Telegram (2834 сообщения, а не 0).

## Проход 1 — нашёл ещё один реальный корень потерь

По полной статистике `hour_stats` в bot_knowledge.json: **час 0 (полночь) — худший час суток**, -0.42 USDT на 5 сделках (1 победа из 5, 20% винрейт). При этом мозг ещё 13 июля добавил этот же час в `bad_hours` после AI-анализа убытка по PYTHUSDT — то есть система формально "знает", что час 0 плохой.

Проверил, что реально делает это знание — и `bad_hours`/`is_good_hour_for_trading()` используются **только в одном месте**: тайминге переключения Tier2/Tier3, и то **только в режиме SNIPER** (функция мгновенно выходит, если режим не SNIPER). А бот всё это время торговал в SMART/SAFE — **100% из 71 сделки** в файле. То есть "мозг знает про плохой час" было чисто декоративной записью, которая никогда не влияла на реальные входы — отсюда час 0 и продолжал терять деньги ровно как до того, как его добавили в bad_hours.

**Исправил** тем же приёмом, что уже работает для уровней ослабления фильтров (v326): перед входом сверяемся со статистикой ТЕКУЩЕГО часа (без ИИ, из собственных данных бота) — при 5+ сделках в этом часе и винрейте <45% требуем от кандидата более высокий score именно сейчас. Работает в любом режиме торговли, не только SNIPER.

## Проход 2 — дочитал выгрузку Telegram (2834 сообщения)

Ничего нового критичного. Единственное, что бросилось в глаза — "аварийная продажа не завершилась полностью" по JTOUSDT (защита из v311), но это сообщение относится к периоду ДО присланного лога — подтвердить или воспроизвести на свежих данных не смог, оставляю на будущее без изменений в коде (защита и так уже есть и работает — просто не на чём было её лишний раз проверить).

---

## Проверка (2 независимых прохода)

```
Проход 1: python3 -m py_compile   → OK
          python3 -m pyflakes | grep "undefined name"   → пусто
Проход 2: python3 -m py_compile   → OK (повторно)
          python3 -m pyflakes | grep "undefined name"   → пусто (повторно)
          ast.parse() — синтаксис файла целиком без аномалий
```

Также прогнал новую логику на реальных цифрах из вашего `hour_stats`: час "0" → WR=20%, n=5 → бонус +1.5 к порогу входа (сработало бы). Час "16" → WR=40%, n=5 → +0.7. Часы с <5 сделок (например "3", "23") — не трогаются, данных мало.

**Процессный момент**: при сборке снова случайно применил правку прямо к уже закоммиченному v332 вместо только v333 — на этот раз поймал сам, ДО отправки (`git status` показал изменения в v332, откатил `git checkout --`). v332, который у вас уже есть, не пострадал и не менялся.

`BOT_VERSION = "v333"`. Код — следующим сообщением.
