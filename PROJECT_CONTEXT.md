# PROJECT_CONTEXT — единая память по проекту торгового бота

> **Назначение этого файла.** Здесь хранится вся суть нашей работы над кодом: что за
> проект, какие версии были, какой баг чем исправлен, какие настройки стоят и какие
> правила мы соблюдаем. Файл лежит в git, поэтому **не теряется между сессиями** —
> если контекст переписки обнулится, начинать надо с чтения этого файла.
> **Файл обновляется каждый раунд** (внизу — журнал изменений по версиям).

Последнее обновление: **2026-07-23** (раунд №55 — отчёт без правок: дыра в защите старых ордеров; v344 не менялся), актуальные версии: `spot_bot_v344_fixed.py` (**v344**) + `ai_brain_advisor_v4.py` (**FIX-v339-THINKING + FIX-v339-RSI-GUARD + FIX-v342-WINDUP**, не менялся в v344).

---

## 1. Что за проект

- **Живой торговый бот на Bybit spot, реальные деньги.** Главный файл — `spot_bot_vNNN_fixed.py` (сейчас v344).
- **Модуль ИИ-советника** — `ai_brain_advisor_v4.py`: ходит в Claude API (`claude-sonnet-5`) для анализа убытков и оценки монет.
- **6 вспомогательных сигнальных ботов** в `aux_bots/`: correlation_bot, funding_bot, liquidation_bot, news_bot, smc_bot, volume_bot.
- **Данные бота** (только на чтение, присылает владелец): `bot_knowledge.json` (история сделок + статистика), `banned_coins.json`, `result.json` (экспорт сообщений Telegram), логи.

**Ветка разработки:** `claude/trading-bot-audit-t4c1u1` в репозитории `sergo9723/footbal-plan-bot`.
Всё коммитим и пушим сюда. **Pull request НЕ создаём**, пока владелец явно не попросит.

---

## 2. Постоянные правила работы (соблюдать всегда)

1. **Только реальные, доказанные фиксы** — каждый баг подтверждаем конкретными строками лога / записями JSON, а не догадками.
2. **Новый версионный файл каждый раунд** для `spot_bot` (`vNNN` → `vNNN+1`). Уже закоммиченные версии **не редактируем на месте** — делаем `cp старый новый`, правим только новый; если старый показал `M` в `git status`, откатываем `git checkout -- старый`.
3. **`ai_brain_advisor_v4.py` правим на месте** (он не версионируется в имени файла, версия — в комментариях-тегах `FIX-vNNN`).
4. **Не перегружать код зря** — не плодить версии и обвязку ради косметики. Явное пожелание владельца.
5. **Отчёт на русском** каждый раунд (`TRADING_BOT_AUDIT_ROUNDNN.md`).
6. **Проверка перед коммитом:** `python3 -m py_compile` + `python3 -m pyflakes | grep "undefined name"` + разбор синтаксиса + поиск задублированных методов.
7. **Отдавать рабочие файлы** через отправку файлов в конце раунда.
8. Владелец пишет по-русски, часто с опечатками — отвечаем по-русски, спокойно и по делу, честно про размер убытков.

---

## 3. Актуальные ключевые настройки (spot_bot v344)

| Параметр | Значение | Смысл |
|---|---|---|
| `BOT_VERSION` | `v344` | текущая версия |
| `FEE_RATE_PCT` | 0.10 | комиссия |
| `BE_LOCK_TRIGGER_PCT` | 0.40 | порог взвода замка прибыли (v344) |
| `BE_LOCK_BUFFER_PCT` | 0.20 | маленький ПЛЮС на откате ~+0.20% net (v344) |
| `BE_LOCK_STRONG_PEAK_PCT` | 0.60 | сильная гарантия с пика 0.60% (v344) |
| `BE_LOCK_STRONG_MIN_NET_PCT` | 0.40 | гарантированный НЕТТО-плюс при сильном пике |
| `TRAIL_DISTANCE_PCT` | 0.40 | поездка: отдаём назад ≤0.40% от пика (v344) |
| `TRAIL_START_PCT` | 0.7 | старт трейлинга |
| `TRAIL_NO_TP1_MULTIPLIER` | 2.0 | множитель трейлинга без tp1 |
| `SCANNER_ADX_MIN` | 35.0 | базовый порог ADX |
| `SCANNER_RSI_MIN / MAX` | 58.0 / 72.0 | окно RSI |
| `MIN_SCORE_TO_ENTER` | 6.5 | минимальный score входа (v344, мягко строже) |
| `AI_LOSS_ANALYSIS_RESERVED_CALLS` | 15 | резерв ИИ-вызовов под анализ убытков |
| `SCANNER_LAST_GOOD_TTL_SEC` | 300 | срок жизни кэша сканера (5 мин) |
| `SCANNER_LAST_GOOD_MAX_DRIFT_PCT` | 0.5 | допустимый дрейф цены в кэше |
| `MAX_COIN_PRICE_USDT` | 5.0 | максимальная цена монеты |
| `DUST_USDT_THRESHOLD` | 3.5 | порог «пыли» |

**ИИ-советник:** модель `claude-sonnet-5`, `MAX_CALLS_PER_DAY = 50`, кэш score 600с, кэш entry 120с.

---

## 4. Журнал версий и что каждая исправила

- **v331** (по логу за месяц, 76 МБ):
  - FIX-v331-1: устаревший `current_price` после AUTO-SWITCH (трейлинг/BE считались по цене старой монеты; pnl показывал +17753%). Обновляем цену перед process_fills/manage + защита по соотношению.
  - FIX-v331-2: сверка PnL по факту (IOC-продажа по bid×(1−slip), а отчёт брал цену на момент решения).
  - FIX-v331-3: сторож дрейфа часов `_check_clock_drift()` (25 443 ошибок ErrCode 10002/мес из-за ~20с дрейфа), бампит recv_window, шлёт совет по NTP.
  - FIX-v331-4: ИИ-парсер — собираем все text-блоки, а не `content[0]['text']`.
  - FIX-v331-5: сброс score для непокупаемого символа (цена > MAX).
  - FIX-v331-25: `_clamp_filters_to_strict_floor()` больше не откатывает лестницу ослабления каждый скан.
- **v332:** закрыт провал покрытия breakeven-lock (мёртвая зона между пиком 0.35% и порогом трейлинга 1.4%, где не было реального ордера на бирже). После полной заливки сетки порог трейлинг-ордера опускается до `BE_LOCK_TRIGGER_PCT`. + показ PnL в процентах.
- **v333:** `bad_hours` (плохие часы мозга) раньше влияли только на тайминг SNIPER. Подключили `hour_stats` к гейтингу score во всех режимах (SMART/SAFE).
- **v334:** `_symbol_loss_streak` был в памяти и обнулялся при рестарте — корень зацикливания на убыточной монете. Теперь сериями убытков засеиваем из истории `bot_knowledge.json` при старте. (2 подряд → запрет 4ч, 3 подряд → бан навсегда.)
- **v335:** ступенчатая фиксация прибыли — при пике ≥0.90% гарантируем НЕТТО ≥+0.40%. + кэш сканера 30мин→5мин с проверкой дрейфа цены.
- **v336:** резерв `AI_LOSS_ANALYSIS_RESERVED_CALLS=15`, чтобы сканер не съедал бюджет анализа убытков; `_call_api(purpose=...)`; `_apply_loss_analysis` шлёт TG при любом сбое (раньше молчал); лог `ai_explain_win` через `logging.getLogger('spotbot')` вместо невидимого `print()`.
- **v337:** FIX-v337-HOLD — перед спуском лестницы: если ТЕКУЩИЙ уровень имеет 8+ сделок и WR≥50%, держим фильтры.
- **v338:** FIX-v338-HOLD2 — перед спуском: если ЦЕЛЕВОЙ уровень имеет 8+ сделок и WR<40%, не спускаемся на него. Завершение правила «не понижать фильтры, если понижение даёт минуса».
- **ai_brain_advisor FIX-v339-THINKING**: корень ошибки «ответ без текстовых блоков: types=['thinking']». У `claude-sonnet-5` расширенное мышление включено по умолчанию; при `max_tokens=600` оно съедало весь бюджет, и в ответе не оставалось text-блока. Добавлен `"thinking": {"type": "disabled"}` в тело запроса — весь бюджет теперь идёт на реальный JSON-ответ, вызов дешевле и быстрее.
- **v339 (раунд №48):** FIX-v339-RSI-GUARD. По скриншотам 18.07: ИИ-анализ убытка MEGAUSDT впервые сработал в бою (диагноз + ПРИМЕНЕНО: ADX 30→33, RSI_MAX 72→65, bad_hours +[11,23]), но через минуту лестница ослабления откатила RSI (RSI_MAX 65→66, RSI_MIN 50→46) — 24ч-защита от отката существовала только для ADX. Теперь ИИ-ужесточения RSI тоже защищены 24ч: советник ставит `_ai_rsi_min_floor`/`_ai_rsi_max_ceil` (+ts), лестница и filter-health их уважают. Ручные /set и /reset_filters не ограничены. Сим-прогон №1 расширен до 10 сценариев (полный путь _apply_loss_analysis), прогон №2 сам находит новейшую версию бота.
- **v340 (раунд №49, по логу v338 за 17-18.07):** FIX-v340-CLAMP-ADX. Корень «20 часов без сделок»: клемп `_clamp_filters_to_strict_floor` каждый скан молча поднимал ADX к строгому полу 28 — шаги лестницы 26/25 и даже ЭКСТРЕННЫЙ сброс до 22 были косметикой (лог: 09:10 «ADX 28→22», 09:47 снова «28→22»; 10:25 ставит 26 → 10:27 снова 28). v331-25 лечил это для RSI_MIN/оборота, но ADX исключили по ошибочному основанию (пол ИИ защищает только повышение от ИИ, не ослабление от клемпа). Фикс: при активном `_filter_relaxed_by_brain` пол клемпа для ADX = 22 (минимум v294-C6 сохранён); шаг лестницы RSI_MAX ограничен реальным потолком 72 (честные сообщения). Сим-прогон №2 расширен частью Ж (3 проверки клемпа), итого 15 проверок. Также по логу: защита ADX-пола от ИИ подтверждена в бою; ИИ-цепочка работает; дрейф часов сервера 7-9с — владельцу нужно включить NTP.
- **v341 (раунд №50, по свежим bot_knowledge/banned_coins):** FIX-v341-STATS — санация symbol_stats при старте: у 8 монет счётчики не сходились с историей (JUPUSDT wins=4 на 2 сделках = «WR 200%»; повреждены только монеты, торговавшиеся до v331 — наследие старого двойного счёта), а мозг читал их для шпаргалки/min_safe_adx/доверия. Пересчёт из полной истории trades[], идемпотентно, проверено на реальном файле владельца. Также в раунде: HOLD не срабатывал ПРАВИЛЬНО («строгое» упало до WR=40% после 2 убытков — держать нечего); be_lock-статистика: 19 выходов, 15 в минус, −0.76 USDT, но 14/15 — на версиях до v335-защиты; при открытой позиции бот не сканирует (подтверждено логом); бан CAPUSDT за 4 подряд убытка — v334 работает. По следующему логу проверить строки «(trail no-tp1)».
- **v342 (раунд №51, по 4-дневному прогону v341, 19-23.07):** FIX-v342-WINDUP — два новых реальных бага. (1) `ai_explain_win()` дублировал счёт побед в symbol_stats при каждом TP_fill/BE_fill (те же победы уже посчитаны `_update_stats()` чуть раньше в том же вызове) — доказано на XANUSDT (1 сделка → wins=2), SPXUSDT, JUPUSDT; убрано. (2) `apply_learning()` каждые 100 сканов безусловно перезаписывал bad_hours из hour_stats (порог 5+ сделок/час), стирая часы, добавленные ИИ после убытков на выборке 1-3 сделки (подтверждено: ИИ применил +[13],+[12],+[22],+[15,19,3,11,6] за 4 дня, apply_learning в те же дни печатал только «[0]»/«[0,13]») — тот же класс бага, что чинили для ADX/RSI в v339/v340. Фикс: ИИ метит добавленные часы `_ai_bad_hours_ts` (24ч), apply_learning объединяет вместо перезаписи. Итоги прогона: 14 сделок за 4 дня (8 побед/6 убытков, но PnL суммарно -0.15 — убытки крупнее побед), relax_level_stats показал «строгое»=худший уровень (WR 38.5% на 13 сделках) — HOLD корректно не срабатывал; smc-сигнальный бот даёт 84% входов при profit factor 0.52 (главный рычаг для будущей доработки, важнее тонких настроек фильтров); ИИ отработал все 6 убытков без единого сбоя thinking-бага. Добавлен sim_run3_v342.py (4 проверки) в audit_tools/.
- **v343 (раунд №52):** FIX-v343-SMCWEIGHT. Владелец спросил прямо: «файл smc менять?» — ответ: нет, баг был в spot_bot. `update_bot_trust()`: ветка `weight=0.3` для явно убыточного сигнального бота была математически недостижима (`elif wr>=0.40 or not truly_profitable: 0.7` ловит всё раньше, чем `else: 0.3`, который требует одновременно wr<0.40 И truly_profitable=True — а truly_profitable сам требует wr>=0.50, несовместимо). smc-бот: 84% всех входов, WR=38.2%, Profit Factor=0.52 — вес навсегда застревал на 0.7 (выше порога 0.5, за которым сигнал игнорируется). Условие исправлено на `wr<0.40 or pf<0.7`. Новый sim_run4_v343.py (5 проверок) воспроизводит точные цифры smc из прогона — вес корректно 0.3 и держится, не откатывается. 34 проверки во всех 4 прогонах — зелёные. ai_brain_advisor_v4.py не менялся.
- **v344 (раунд №54):** реализована идея владельца — «на откате с +0.40% забрать маленький ПЛЮС, а не дать уйти в минус; на крупном движении (пик +1%+) дать поездку и забрать +0.8-1.5%». Вся машинерия трейлинга уже была (v331/v332/v335) — ТОЛЬКО настройка параметров, нового кода нет. BE_LOCK_TRIGGER 0.35→0.40, BUFFER 0.15→0.20 (маленький плюс вместо безубытка), STRONG_PEAK 0.90→0.60 (средний пик по данным=0.69%, порог 0.90 не срабатывал), TRAIL_DISTANCE 0.35→0.40 (поездка). + мягко MIN_SCORE 6.0→6.5 (качество входа). Прогон на 24 реальных be_lock-сделках из истории (значения из Config v344): сумма −0.94$→+0.54$, плюсовых 6/24→21/24, +1.47$. Оговорки: прогон оптимистичен (чистое исполнение лимитки, вычтено 0.05% проскальзывания); спасает только сделки, что СНАЧАЛА выросли до +0.40% (мгновенные dump — нет, им помогает MIN_SCORE). Новый sim_run5_v344.py в audit_tools/ (5 прогонов всего). Следующий шаг: прогон на живых сделках, прислать лог + bot_knowledge.
- **Раунд №47** (без изменений кода): введён постоянный шаблон проверки `AUDIT_CHECKLIST.md` (5 проходов: механика всех 8 файлов → AST → логика правок → прогон ИИ-модуля → прогон торговой цепочки) + два повторно запускаемых симулятора в `audit_tools/` (`sim_run1_advisor.py` — 9 сценариев API на реальном AIBrainAdvisor; `sim_run2_spotbot.py` — 12 проверок на реальном BotKnowledge: relax_level_stats/HOLD/HOLD2/серии убытков/be_lock/hour_stats/персистентность). Все проверки зелёные, v338 + FIX-v339-THINKING подтверждены симуляцией. **С этого раунда каждая проверка идёт строго по шаблону AUDIT_CHECKLIST.md.**

---

## 5. Аварийные защиты от «залипания» (не дают боту встать намертво)

- streak ≥ 50 → экстренный сброс (ADX→22, turnover→50k);
- 24ч без сделок → агрессивный сброс;
- 48ч без сделок → полный сброс.
- Механизмы HOLD/HOLD2 (v337/v338) только придерживают спуск на слабые уровни, но всегда уступают этим аварийным клапанам. Бот не может простоять >24ч без торговли.

---

## 6. Открытые задачи / на будущее

- **[ВАЖНО, безопасность] Ротация ключей.** В aux-ботах ранее были захардкожены API-ключ/секрет Bybit и токен Telegram-бота. Их нужно **сменить** в кабинете Bybit и у @BotFather — из репозитория/логов их видно. Пока не подтверждено, что сделано.
- **[ЗАКРЫТО в раунде №51] 4-дневный прогон v341 (19-23.07) проанализирован.** Итог: 14 сделок (8W/6L, PnL −0.15, убытки крупнее побед), relax_level_stats показал «строгое»=худший уровень (не баг — HOLD корректно не держал), ИИ отработал 6/6 убытков без сбоев, smc-сигнальный бот даёт 84% входов при profit factor 0.52 (главный рычаг на будущее). Два новых бага найдены и исправлены в v342 (двойной счёт побед + стирание bad_hours мозгом). Полный разбор — `TRADING_BOT_AUDIT_ROUND51.md`.
- **[ОТКРЫТО, на будущее] Пересмотр роли smc-бота и соотношения риск/прибыль** — из рекомендаций раунда №51: smc даёт 84% входов, но profit factor 0.52 (теряет деньги); WR 46-57% но убытки в среднем крупнее побед. Ждём решения владельца: ограничить вход по силе smc-сигнала, либо чаще пускать сканер торговать самостоятельно; пересмотреть SL/TP соотношение.
- **[ОТКРЫТО, найдено в раунде №54, отчёт без правок] Нет защиты от старой неисполненной BUY-сетки.** По скринам ARXUSDT (22:41) ордера висели без залива; код НЕ имеет таймаута «ордер висит N минут не исполнился → отменить». `_last_grid_built_ts` есть, но используется только для блока SMC-switch (120с), не для отмены по времени. Отмена/пересборка сетки — только по движению цены (GRID_REBUILD_THRESHOLD=2%), не по времени. prebuy 1H-check работает только ДО постановки сетки, не при утреннем заливе. Защита ЕСТЬ от «цена ушла» (rebuild 2%) и «резкий обвал» (falling_grid 2 залива<5мин) + стоп-лосс 0.8% после залива. Предложено (не реализовано): таймаут возраста неисполненной сетки (30-45мин) → отмена + пересборка по свежему тренду; разовая перепроверка 1H-тренда при первом заливе после долгого простоя. Ждём «делай» от владельца → v345. Также в раунде: ADX упал до 19 (штатный шаг лестницы, v340 защищал только от подъёма клемпом) — в медвежьем рынке рискованно, но EMA1H↓/EMA15m↓ гейты вход заблокировали, убытка не было.
- Продолжить наблюдение ещё несколько дней на v342 — оба новых фикса (двойной счёт, bad_hours) впервые в бою.
- **[РАУНД №53, 2026-07-23, повторный полный аудит обоих кодов] Кода-багов НЕ найдено — новую версию НЕ делал** (не перегружаю код зря). Загруженные владельцем файлы побитно идентичны репозиторию (v343 + ai_brain_advisor) — установлена последняя версия. Все 34 автопроверки (4 прогона) зелёные; фикс v343 (вес smc) прослежен по обоим путям входа — реально блокирует сигнал (`continue` на строке ~19892 при weight<0.5, буст только при weight≥0.8 на ~16973). **Математический корень минуса подтверждён цифрами 91 сделки:** WR=46.2%, средняя победа +0.065, средний убыток −0.088 (убытки в 1.35× крупнее) → для нуля нужен WR≥57.4%, а он 46%. Только 13% сделок (12/91) доходят до реального TP; крупнейшие source убытка: full_grid_early_stop (−1.67 на 13 сделках) и be_lock (−0.95 на 25). Это СТРАТЕГИЯ, не баг. Владельцу задан вопрос (AskUserQuestion) о направлении: (A) дать v343 поработать и оценить эффект smc-гейтинга; (B) сделать входы строже (меньше сделок, выше качество); (C) пересмотреть SL/TP соотношение. Ждём выбор перед след. изменением кода.

---

## 7. Как пользоваться этим файлом (для будущих сессий)

1. Прочитать этот файл целиком — это и есть память по проекту.
2. Свежие детали каждого раунда — в соответствующем `TRADING_BOT_AUDIT_ROUNDNN.md`.
3. После каждого раунда: добавить строку в раздел 4 (журнал версий) и обновить дату сверху.
