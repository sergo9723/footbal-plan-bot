# Аудит №48 (v339) — ИИ заработал живьём, и ваши скриншоты сразу нашли новый корень: лестница откатывала решения ИИ по RSI

## Что показали ваши скриншоты 18.07 — по порядку

**1. ИИ-анализ убытков заработал полностью.** Впервые в бою видна вся цепочка: убыток MEGAUSDT → «🤖 ИИ-АНАЛИЗ УБЫТКА» с настоящим диагнозом («вход в 11ч — стабильно плохой час WR=33%, RSI=67.3 близко к перекупленности, ранний стоп за 0.23ч без шанса на разворот») → «✅ ПРИМЕНЕНО (65%)»: ADX_MIN 30→33, RSI_MAX 72→65, плохие часы +[11, 23]. Фикс thinking из прошлого раунда подтверждён в реальной торговле. Расход — всего 4 вызова за день.

**2. Но через ОДНУ МИНУТУ лестница откатила часть решения ИИ.** В 11:17 ИИ сузил RSI_MAX до 65 (именно потому, что вход при RSI=67.3 дал убыток). В 11:18 «Мозг ослабляет фильтры (максимальное, скан №25)»: **RSI_MAX 65→66, RSI_MIN 50→46**. И с каждым следующим шагом лестницы RSI продолжал бы расползаться.

## Корень (проверен по коду, не догадка)

Защита «лестница не может откатить решение ИИ» (24-часовой пол, v325/v326) была построена **только для ADX** (`_ai_adx_floor` — 3 места в коде). Для RSI_MIN/RSI_MAX, которые ИИ меняет точно так же, никакой защиты не существовало вовсе. Это ровно тот же класс бага, что уже ловили по PYTHUSDT с ADX — но для другого параметра.

Разобраны все 6 мест, где код автоматически меняет RSI:
- лестница ослабления (поднимает RSI_MAX, опускает RSI_MIN) — **откатывала ИИ** → защищено;
- «здоровье фильтров» (опускает RSI_MIN при блокировке рынка) — **могла откатить ИИ** → защищено;
- 2 места учёбы мозга на убытках при RSI>68 (сужают RSI_MAX) — двигают в ту же сторону, что и ИИ → не тронуты;
- команды `/set rsi` и `/reset_filters` — ручные, владелец главнее любой защиты → не тронуты.

## Фикс FIX-v339-RSI-GUARD

**В `ai_brain_advisor_v4.py`** (оба пути — анализ убытка и ночной отчёт): когда ИИ УЖЕСТОЧАЕТ RSI (поднимает RSI_MIN или опускает RSI_MAX), выставляются `_ai_rsi_min_floor` / `_ai_rsi_max_ceil` с меткой времени — зеркально существующему `_ai_adx_floor`.

**В `spot_bot_v339_fixed.py`**: лестница и «здоровье фильтров» не могут в течение 24ч опустить RSI_MIN ниже пола ИИ или поднять RSI_MAX выше потолка ИИ. Через 24ч защита истекает сама (как у ADX) — если рынок изменился, бот не остаётся зажатым навсегда.

По вчерашнему сценарию: ИИ ставит RSI_MAX=65 → лестница хочет 66 → останется 65 до истечения суток. Ослабление оборота/стакана/ADX (до пола) лестнице по-прежнему доступно — бот не встанет.

## Проверка по шаблону AUDIT_CHECKLIST (все пункты)

- **Проход 1 (механика):** py_compile + pyflakes «undefined name» — spot_bot_v339 и ai_brain_advisor чисто ✅
- **Проход 2 (AST):** дубли методов / голые except / изменяемые дефолты — чисто ✅
- **Проход 3 (логика):** все 4 точки фикса найдены тегом FIX-v339-RSI-GUARD (4 в боте, 4 в советнике); направление clamp'ов проверено (min для потолка, max для пола); ручные команды не ограничены ✅
- **Проход 4 (прогон №1, ИИ-модуль):** теперь **10 сценариев** — добавлен №7: полный путь `_apply_loss_analysis` с ответом ИИ «ADX 33, RSI_MIN 44, RSI_MAX 65» → выставлены floor=44, ceil=65, adx_floor=33 с метками времени. **10/10** ✅
- **Проход 5 (прогон №2, торговая цепочка):** симулятор теперь сам находит новейшую версию (проверил именно v339) — все 12 проверок ✅
- **Финал:** в git-статусе только задуманные файлы, v338 не тронут; секретов нет ✅

## Ответ на вопрос «а почему бот вообще вошёл в 11ч, если это плохой час?»

До этой сделки час 11 ещё не был в стоп-списке (он добавляется после 2+ убытков — что и произошло сейчас: «Час 11:00 → добавлен в стоп-список»). Плюс ИИ добавил [11, 23] в плохие часы. Повторный вход в 11ч теперь дорогой вдвойне: и стоп-список часа, и bad_hours от ИИ.

`BOT_VERSION = "v339"`. Файлы — следующим сообщением. Обновить нужно ОБА: и spot_bot_v339, и ai_brain_advisor_v4 (защита работает в паре: советник ставит пол/потолок — бот их уважает), затем перезапустить.
