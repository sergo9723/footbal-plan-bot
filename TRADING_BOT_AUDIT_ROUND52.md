# Аудит №52 (v343) — файл smc менять не нужно было: баг был в spot_bot, в системе доверия к сигналам

## Прямой ответ на ваш вопрос

**`aux_bots/smc_bot.py` менять не нужно.** Он только ищет паттерны (FVG, слом структуры, зоны ликвидности) и присылает сигнал — сам не следит за своей точностью и не должен. За то, сколько **доверия** давать его сигналам по факту результатов, отвечает отдельная система в `spot_bot` — `update_bot_trust()`. Проверил её построчно и нашёл в ней реальный баг.

## Что предпринял в прошлом раунде — честно: ничего, только диагноз

В отчёте №51 я написал «smc даёт 84% входов, profit factor 0.52» как рекомендацию на будущее, но **не вносил правку**. Вы правы, что переспросили. Сейчас — реальный фикс.

## Найденный баг: вес доверия к убыточному боту не мог упасть никогда

В коде есть система весов (0.3–1.5) — чем хуже бот торгует, тем меньше его сигналы влияют на входы; при весе <0.5 сигнал вообще игнорируется. Формула:

```python
if   wr>=0.70 and pf>=1.5 and truly_profitable: weight = 1.5
elif wr>=0.60 and pf>=1.2 and truly_profitable: weight = 1.2
elif wr>=0.50 and pf>=1.0 and truly_profitable: weight = 1.0
elif wr>=0.40 or not truly_profitable:          weight = 0.7   ← здесь ошибка
else:                                            weight = 0.3   ← сюда попасть НЕВОЗМОЖНО
```

Ветка `weight = 0.3` требует одновременно `wr < 0.40` И `truly_profitable = True`. Но `truly_profitable` сам требует `wr ≥ 0.50`. `wr < 0.40` и `wr ≥ 0.50` не могут быть правдой одновременно — **условие математически невыполнимо**. Из-за союза «or» в строке выше любой недостаточно прибыльный бот (а таких — почти все) попадает в ветку 0.7 раньше, чем доходит до 0.3.

**На smc это било напрямую:** 76 из 91 сделки (84% всех входов), винрейт 38.2%, Profit Factor 0.52 (на каждый доллар прибыли — почти два доллара убытка) — а вес всё равно навсегда стоял на 0.7, выше порога 0.5, за которым сигнал бы игнорировался. Был отдельный аварийный оверрайд («WR<35% → weight=0.3 принудительно»), который однажды сработал в логе — но следующая же сделка снова пересчитывала вес по сломанной формуле и откатывала обратно на 0.7. Плохие сигналы smc продолжали влиять на входы бота все 4 дня без исключения.

## Фикс v343

Условие исправлено на `wr < 0.40 or pf < 0.7` — теперь явно убыточный бот (по винрейту ИЛИ по соотношению прибыль/убыток) реально получает вес 0.3 и **удерживает** его при каждом следующем пересчёте, а не соскальзывает обратно. Смысл не менялся — только починена сама логика: другие 4 уровня весов (1.5/1.2/1.0/0.7) не тронуты.

## Проверка — на точных цифрах вашего прогона

Прогон №4 (новый, `sim_run4_v343.py`) воспроизвёл ровно ситуацию smc (76 сделок, WR=38.2%, PF=0.52) на реальном коде — вес корректно получился 0.3 и не откатился после следующей сделки; порог игнорирования сигнала (<0.5) реально срабатывает. Заодно проверил, что сильный бот по-прежнему получает 1.5, а посредственный (WR≈46%, PF≈0.95, ни явно хороший, ни явно плохой) остаётся на 0.7 — регресса нет.

- Прогон №1 (ИИ-модуль): 10/10 ✅
- Прогон №2 (торговая цепочка): 15/15 ✅
- Прогон №3 (v342 фиксы): 4/4 ✅
- **Прогон №4 (вес доверия, новый): 5/5** ✅
- Механика/AST на v343: чисто ✅

**Итого 34 автоматических проверки на реальном коде — все зелёные.**

## Чего ждать после установки v343

Как только smc наберёт очередные 5+ сделок на текущей плохой статистике (WR/PF не улучшатся сами) — вес упадёт до 0.3 и **останется там**. С этого момента заработает уже существующий, но бездействовавший механизм: «SMC сигнал ИГНОРИРОВАН: weight=0.3<0.5» — сканер начнёт чаще искать монеты самостоятельно, по своим фильтрам, а не по сигналам бота с доказанно плохим соотношением прибыль/убыток. Это не гарантия прибыли, но убирает конкретный, найденный источник систематических потерь.

`BOT_VERSION = "v343"`. Файл — следующим сообщением (ai_brain_advisor_v4.py в этом раунде не менялся — обновлять его не нужно, у вас уже последняя версия с прошлого раза).
